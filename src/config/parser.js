'use strict';

//  Ref: https://www.npmjs.com/package/csv-parser
module.exports = {
	// escape: '"',
	// headers: [],
	//  These two options would get used on a per-model basis, so they shouldn't really exist here.
	// mapHeaders: () => {},
	// mapValues: () => {}
	//  The parser is going to accumulate data in memory, so we might need to batch it out if they are massive file inputs
};
